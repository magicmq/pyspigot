{
    "id": "${project.pluginNameLowerCase}",
    "name": "${project.pluginName}",
    "version": "${project.version}",
    "description": "${project.description}",
    "url": "${project.url}",
    "authors": ["magicmq"],
    "main": "dev.magicmq.pyspigot.velocity.PyVelocity",
    "dependencies": [
        {
            "id": "packetevents",
            "optional":true
        }
    ]
}